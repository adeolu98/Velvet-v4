{
  "name": "Meta-Aggregator Swap Contracts",
  "scripts": {
    "test": "hardhat deploy --reset && hardhat test",
    "coverage": "hardhat deploy --reset &&  hardhat coverage --solcoverjs ./.solcover.js"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@openzeppelin/contracts": "^5.0.2",
    "hardhat": "^2.22.15",
    "hardhat-deploy": "^0.11.45"
  },
  "dependencies": {
    "bignumber.js": "^9.1.2",
    "dotenv": "^16.4.5"
  }
}
